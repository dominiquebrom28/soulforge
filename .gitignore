# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env (keep the example committed)
.env
.env.*
!.env.example

# Claude Code local overrides
.claude/settings.local.json

# GandalfHardcore asset pack — license forbids redistribution; keep it out of the
# (public) repo. The app references it locally; make the repo private to commit it.
public/assets/gandalf/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
