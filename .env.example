# Supabase — copy to .env.local and fill in (Project Settings → API).
# The anon key is the PUBLIC client key (safe in the frontend, protected by RLS).
# NEVER put the service_role secret key here.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
